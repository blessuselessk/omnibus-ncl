# porkg-ncl library — re-exports all contracts and builders
{
  namespace = import "namespace.ncl",
  proto = import "proto.ncl",
  privilege = import "privilege.ncl",
  worker = import "worker.ncl",
  job = import "job.ncl",
  config = import "config.ncl",
  builders = import "builders.ncl",
}
