# prose-ncl library — re-exports contracts and builders
{
  primitives = import "primitives.ncl",
  skill = import "skill.ncl",
  workspace = import "workspace.ncl",
  graph = import "graph.ncl",
  builders = import "builders.ncl",
}
