# codemode-nickel library — contracts and builders for Nickel project generation
{
  schema = import "schema.ncl",
  tool = import "tool.ncl",
  nickel = import "nickel.ncl",
  project = import "project.ncl",
  builders = import "builders.ncl",
}
