{
  event = import "event.ncl",
  hat = import "hat.ncl",
  backpressure = import "backpressure.ncl",
  loop = import "loop.ncl",
  skill = import "skill.ncl",
  config = import "config.ncl",
  builders = import "builders.ncl",
  deploy = import "deploy.ncl",
}
