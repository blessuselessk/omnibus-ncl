# process-compose-ncl library — re-exports all contracts, builders, and bridge
{
  probe = import "probe.ncl",
  process = import "process.ncl",
  config = import "config.ncl",
  builders = import "builders.ncl",
  bridge = import "bridge.ncl",
}
